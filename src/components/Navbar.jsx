import React from 'react';
import Logo from '../assets/logo.svg';

    const Navbar = () => {
        return (
            <nav>
                <img src={Logo} alt='sneakers store logo' />
            </nav>
        );
    };

    export default Navbar;